-- Add a path first, then its sub_paths, stages, and resources using the IDs returned by your database.
-- insert into paths (title, slug, description) values ('Path title', 'path-slug', 'Description');

